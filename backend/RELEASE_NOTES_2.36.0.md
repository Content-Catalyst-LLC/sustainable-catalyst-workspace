# Workspace v2.36.0 — Production Architecture Certification

Certifies the backend-native Workspace v2 architecture before the v3.0 milestone. Adds a protected production-architecture certification endpoint, typed client contract, WordPress proxy surface, deployment certification gates, and release/package reconstruction checks. No database migration is introduced; migration lineage remains at 031. Live public-page/cache/rollback field checks remain explicit and are not silently inferred.
