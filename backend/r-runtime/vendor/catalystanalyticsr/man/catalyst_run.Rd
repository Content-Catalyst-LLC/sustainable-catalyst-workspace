% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalyst_run.R
\name{catalyst_run}
\alias{catalyst_run}
\title{Run a complete Catalyst Analytics scenario}
\usage{
catalyst_run(
  times,
  x0,
  policy = NULL,
  params = list(),
  scenario = "baseline",
  model = "khncpa",
  model_version = NULL,
  method = c("rk4", "euler"),
  emissions_budget = NULL,
  carbon_budget_value = NULL,
  include_phase_plane = TRUE,
  include_sensitivity = TRUE
)
}
\arguments{
\item{times}{Numeric vector of times.}

\item{x0}{Named numeric vector of initial states (K,H,N,C,P,A).}

\item{policy}{List of policy controls (s,e,a).}

\item{params}{List of model parameters.}

\item{scenario}{Scenario label.}

\item{model}{Registered model id or \code{catalyst_model} object.}

\item{model_version}{Optional exact model version.}

\item{method}{Integration method.}

\item{emissions_budget}{Optional single numeric budget for total allowed cumulative emissions (same units as the model's `emissions`). If provided, carbon_budget() is computed.}

\item{carbon_budget_value}{Deprecated alias for `emissions_budget` (kept for backwards compatibility).}

\item{include_phase_plane}{Logical. If TRUE, compute a phase plane.}

\item{include_sensitivity}{Logical. If TRUE, compute Jacobian sensitivities.}
}
\value{
A list with class "catalyst_run".
}
\description{
This is the "front door" for the package: one function that runs the model,
produces tidy outputs, makes plots, and returns a single structured result.
}
\examples{
times <- seq(0, 10, by = 1)
x0 <- c(K = 1, H = 1, N = 1, C = 0, P = 1, A = 1)
x <- catalyst_run(times, x0, include_phase_plane = FALSE, include_sensitivity = FALSE)
plot(x, which = "trajectory")
}
