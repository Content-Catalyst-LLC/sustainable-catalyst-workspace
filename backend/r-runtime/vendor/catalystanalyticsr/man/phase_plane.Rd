% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase_plane.R
\name{phase_plane}
\alias{phase_plane}
\title{Compute a 2D phase plane (vector field) for the KH-NC-PA model}
\usage{
phase_plane(
  x_var = "K",
  y_var = "C",
  x_range = c(0.5, 3),
  y_range = c(-1, 3),
  n = 15,
  t = 0,
  state_fixed = c(K = 1, H = 1, N = 1, C = 0, P = 1, A = 1),
  policy = list(s = 0.2, e = 0.05, a = 0),
  params = list(),
  normalize = TRUE
)
}
\arguments{
\item{x_var}{Character. State name for x-axis (default "K").}

\item{y_var}{Character. State name for y-axis (default "C").}

\item{x_range}{Numeric length-2. Range for x grid.}

\item{y_range}{Numeric length-2. Range for y grid.}

\item{n}{Integer. Grid resolution per axis.}

\item{t}{Numeric. Time at which to evaluate the vector field.}

\item{state_fixed}{Named numeric vector. Baseline values for all states.}

\item{policy}{List of policy controls (s, e, a) as in simulate_dynamics().}

\item{params}{List of parameters (overrides defaults).}

\item{normalize}{Logical. If TRUE, scales arrows to unit length for plotting.}
}
\value{
A data.frame with columns x, y, dx, dy, dx_raw, dy_raw.
}
\description{
This is a utility for visualization: pick two state dimensions (e.g., K vs C)
and hold the other state variables fixed.
}
\examples{
times <- seq(0, 5, by = 1)
x0 <- c(K = 1, H = 1, N = 1, C = 0, P = 1, A = 1)
base <- simulate_dynamics(times, x0, return_long = FALSE)

state0 <- as.numeric(base$trajectory_wide[1, c("K","H","N","C","P","A")])
names(state0) <- c("K","H","N","C","P","A")

pp <- phase_plane(
  x_var = "K",
  y_var = "C",
  x_range = range(base$trajectory_wide$K, na.rm = TRUE),
  y_range = range(base$trajectory_wide$C, na.rm = TRUE),
  n = 10,
  t = times[1],
  state_fixed = state0,
  policy = list(s = 0.20, e = 0.05, a = 0.00),
  params = list(),
  normalize = TRUE
)
head(pp)
}
