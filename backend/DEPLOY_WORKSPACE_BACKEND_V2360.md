# Deploy Workspace Backend v2.36.0

Deploy backend first using `backend/deploy_workspace_backend_v2_36_0_vps.sh`, then install the WordPress package and apply the Git patch. No new migration is introduced; existing migrations through 031 are replay-safe.
