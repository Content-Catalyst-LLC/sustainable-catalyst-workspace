# Deploy Workspace backend v2.27.0

Run `deploy_workspace_backend_v2_27_0_vps.sh` against the v2.27.0 backend ZIP. The installer preserves the prior `.env`, applies migration 027, rebuilds the inherited bounded runtime fabric, validates the v2.26 notebook orchestration contract with authenticated service identity, then creates and deep-verifies a reproducible scientific study package.
