# Deploy Workspace backend v2.30.0

Deploy backend first with `deploy_workspace_backend_v2_30_0_vps.sh`. No new database migration is introduced; migration lineage remains through 028. The deployer verifies the typed-client contract plus the thin-client profile and server-generated bootstrap projection.
