from pathlib import Path
import json,sys
ROOT=Path(__file__).resolve().parents[1]
def check(ok,label):
    if not ok: print('FAIL - '+label);sys.exit(1)
    print('PASS - '+label)
main=(ROOT/'wordpress/sustainable-catalyst-workspace/sustainable-catalyst-workspace.php').read_text()
php=(ROOT/'wordpress/sustainable-catalyst-workspace/includes/class-sc-workspace.php').read_text()
reg=(ROOT/'wordpress/sustainable-catalyst-workspace/includes/class-sc-workspace-registry.php').read_text()
js=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/js/workspace-v0.28.0.js').read_text()
audit=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-audit-trail-v1.js').read_text()
css=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/css/workspace-v0.28.0.css').read_text()
manifest=json.loads((ROOT/'release-manifest-v0.28.0.json').read_text())
record=json.loads((ROOT/'registry/workspace-product-record-v0.28.0.json').read_text())
check('Version: 0.28.0' in main,'plugin version')
check("define('SC_WORKSPACE_VERSION', '0.28.0')" in main,'runtime version')
check(manifest['version']=='0.28.0' and manifest['previous_version']=='0.27.0','manifest release lineage')
check(manifest['release_name']=='Project Audit Trail & Governance Ledger','release name')
check(manifest['storage_schema_version']==26 and manifest['project_schema']=='sc-workspace-project/11.0','schema-stable storage 26 / project 11.0')
check(not manifest['schema_migration_required'] and manifest['migration']['storage_from']==26 and manifest['migration']['storage_to']==26,'no data migration')
check("'/audit-trail-contract'" in php and 'public function audit_trail_contract()' in php,'Audit Trail REST contract')
check(manifest['audit_trail_schema']=='sc-workspace-audit-trail/1.0' and manifest['audit_event_schema']=='sc-workspace-audit-event/1.0','Audit Trail schemas')
check('data-scw-workspace-view="audit"' in php and 'data-scw-workspace-section="audit"' in php,'Audit workspace view')
check('SCWorkspaceAuditTrail' in js and 'renderAuditTrail' in js,'Audit UI runtime')
check('derive' in audit and 'exportPackage' in audit and 'storedShadowDatabase:false' in audit,'derived audit helper')
check(manifest['audit_trail']['derived_from_authoritative_ledgers'] and not manifest['audit_trail']['stored_shadow_database'],'authoritative-ledger derivation / no shadow database')
check(not manifest['audit_trail']['hidden_governance_score'] and not manifest['audit_trail']['people_ranking'],'no hidden governance score or people ranking')
check(not manifest['audit_trail']['project_content_in_export'] and 'projectContentIncluded:false' in audit,'privacy-minimized audit export')
check('data-storage-version="26"' in php,'rendered storage version corrected')
check('/* v0.28.0 — Project Audit Trail & Governance Ledger */' in css,'Audit visual layer')
check(manifest['canonical_library_path']=='/knowledge-libraries/' and '/knowledge-libraries/' in php,'canonical Knowledge Library route')
check(record['public_version']=='0.28.0' and record['previous_version']=='0.27.0','registry record lineage')
check("BACKUP_KEY = 'sc_workspace_registry_backup_v0280'" in reg and "PENDING_KEY = 'sc_workspace_registry_pending_v0280'" in reg and 'LEGACY_PENDING_KEY_V0270' in reg,'registry pending/backup lineage')
check((ROOT/'history/release-manifest-v0.27.0.json').exists() and (ROOT/'history/workspace-product-record-v0.27.0.json').exists(),'v0.27.0 history preserved')
json.loads((ROOT/'schemas/sc-workspace-audit-event-v1.schema.json').read_text()); json.loads((ROOT/'schemas/sc-workspace-audit-export-v1.schema.json').read_text())
check(True,'Audit schema JSON')
print('VALIDATION PASSED: Sustainable Catalyst Workspace v0.28.0 — Project Audit Trail & Governance Ledger')
