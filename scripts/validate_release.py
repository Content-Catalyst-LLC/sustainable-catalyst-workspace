#!/usr/bin/env python3
import json
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1];MAN=json.loads((ROOT/'release-manifest-v0.38.0.json').read_text());REG=json.loads((ROOT/'registry/workspace-product-record-v0.38.0.json').read_text());MAIN=(ROOT/'wordpress/sustainable-catalyst-workspace/sustainable-catalyst-workspace.php').read_text();PHP=(ROOT/'wordpress/sustainable-catalyst-workspace/includes/class-sc-workspace.php').read_text();JS=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/js/workspace-v0.38.0.js').read_text();NB=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-research-notebook-v7.js').read_text();PORT=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-notebook-portability-v1.js').read_text()
def c(x,l):
 if not x:raise SystemExit('FAIL - '+l)
 print('PASS - '+l)
c('Version: 0.38.0' in MAIN,'plugin version');c((MAN['version'],MAN['previous_version'],MAN['release_name'])==('0.38.0','0.37.0','Portable & Synced Notebooks'),'manifest lineage');c((MAN['storage_schema_version'],MAN['project_schema'])==(34,'sc-workspace-project/19.0'),'storage/project migration');c('function migrateV33(raw)' in JS and 'if (raw.schemaVersion === 33) return migrateV33(raw);' in JS,'browser migration');c(MAN['notebook_workspace_schema']=='sc-workspace-notebook-workspace/7.0','Notebook v7 container');c(all(x in PORT for x in ('portablePackage','validatePortable','importAsCopy','restorePoint','restoreAsCopy','syncPush','revision-precondition')),'portability runtime');c(all(x in PHP for x in ('cloud_notebook_store','cloud_notebooks_list','scw_notebook_sync_conflict','Nothing was overwritten')),'conflict-safe account transport');c(not MAN['governance']['notebook_background_sync'] and not MAN['governance']['notebook_silent_last_write_wins'],'no background sync / no last-write-wins');c((REG['public_version'],REG['previous_version'])==('0.38.0','0.37.0'),'registry lineage');c((ROOT/'history/release-manifest-v0.37.0.json').exists(),'v0.37 history retained')
for p in list((ROOT/'schemas').glob('*.json'))+[ROOT/'release-manifest-v0.38.0.json',ROOT/'registry/workspace-product-record-v0.38.0.json']:json.loads(p.read_text())
print('VALIDATION PASSED: Sustainable Catalyst Workspace v0.38.0 — Portable & Synced Notebooks')
