from pathlib import Path
import json, sys
ROOT=Path(__file__).resolve().parents[1]
def check(ok,label):
    if not ok: print('FAIL - '+label); sys.exit(1)
    print('PASS - '+label)
main=(ROOT/'wordpress/sustainable-catalyst-workspace/sustainable-catalyst-workspace.php').read_text()
php=(ROOT/'wordpress/sustainable-catalyst-workspace/includes/class-sc-workspace.php').read_text()
reg=(ROOT/'wordpress/sustainable-catalyst-workspace/includes/class-sc-workspace-registry.php').read_text()
js=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/js/workspace-v0.34.0.js').read_text()
notebook=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-research-notebook-v3.js').read_text()
css=(ROOT/'wordpress/sustainable-catalyst-workspace/assets/css/workspace-v0.34.0.css').read_text()
manifest=json.loads((ROOT/'release-manifest-v0.34.0.json').read_text())
record=json.loads((ROOT/'registry/workspace-product-record-v0.34.0.json').read_text())
check('Version: 0.34.0' in main and "define('SC_WORKSPACE_VERSION', '0.34.0')" in main,'plugin/runtime version')
check((manifest['version'],manifest['previous_version'])==('0.34.0','0.33.0'),'manifest lineage')
check(manifest['release_name']=='Notebook Collections & Knowledge Linking','release name')
check((manifest['storage_schema_version'],manifest['project_schema'],manifest['export_schema'])==(30,'sc-workspace-project/15.0','sc-workspace-project-export/15.0'),'storage/project/export schemas')
check(manifest['migration']['storage_from']==29 and manifest['migration']['storage_to']==30 and 'function migrateV29(raw)' in js,'29→30 migration runtime')
check(manifest['notebook_workspace_schema']=='sc-workspace-notebook-workspace/3.0' and manifest['notebook_export_schema']=='sc-workspace-notebook-export/3.0','Notebook Workspace v3')
check("'schema' => 'sc-workspace-project-contract/15.0'" in php and "'export_schema' => 'sc-workspace-project-export/15.0'" in php,'Project REST contract v15')
check("array('sc-workspace-project/15.0','sc-workspace-project/14.0','sc-workspace-project/13.0','sc-workspace-project/12.0','sc-workspace-project/11.0')" in php,'account persistence accepts Project v15 + legacy window')
check(manifest['notebook_link_schema']=='sc-workspace-notebook-link/1.0' and manifest['notebook_collection_schema']=='sc-workspace-notebook-collection/1.0','link + collection schemas')
check('createLink' in notebook and 'backlinksForRef' in notebook and 'createCollection' in notebook,'linking helper runtime')
check('data-scw-notebook-link-form' in php and 'data-scw-notebook-collection-form' in php,'linking UI')
check("'automatic_link_inference' => false" in php and not manifest['research_notebook']['automatic_link_inference'],'explicit-only link governance')
check(record['public_version']=='0.34.0' and record['previous_version']=='0.33.0','registry lineage')
check("BACKUP_KEY = 'sc_workspace_registry_backup_v0340'" in reg and 'LEGACY_PENDING_KEY_V0330' in reg,'registry migration lineage')
check((ROOT/'history/release-manifest-v0.33.0.json').exists() and (ROOT/'history/workspace-product-record-v0.33.0.json').exists(),'v0.33 history preserved')
check(':focus-visible' in css and 'prefers-reduced-motion:reduce' in css,'accessibility retained')
for f in ('sc-workspace-notebook-ref-v1.schema.json','sc-workspace-notebook-link-v1.schema.json','sc-workspace-notebook-collection-v1.schema.json','sc-workspace-notebook-workspace-v3.schema.json','sc-workspace-notebook-export-v3.schema.json','sc-workspace-project-v15.schema.json'):
    json.loads((ROOT/'schemas'/f).read_text())
check(True,'v0.34 schema JSON')
print('VALIDATION PASSED: Sustainable Catalyst Workspace v0.34.0 — Notebook Collections & Knowledge Linking')
