(function(root,factory){
  const api=factory();
  if(typeof module==='object'&&module.exports)module.exports=api;
  else { root.SCWorkspaceApiEmbed=api; api.autoRender(); }
})(typeof self!=='undefined'?self:this,function(){
  'use strict';
  const DURABLE_REF_SCHEMA='sc-workspace-durable-reference/1.0';
  const PROJECTION_SCHEMA='sc-workspace-readonly-projection/1.0';
  const API_SCHEMA='sc-workspace-readonly-api-envelope/1.0';
  const EMBED_SCHEMA='sc-workspace-embed-descriptor/1.0';
  const LEDGER_SCHEMA='sc-workspace-api-embed-ledger/1.0';
  const SAFETY_REPORT_SCHEMA='sc-workspace-integration-safety-report/1.0';
  const HARDENING_SCHEMA='sc-workspace-api-embed-hardening/1.0';
  const STORAGE_KEY='sc_workspace_api_embed_v1';
  const MAX_EMBED_BYTES=98304;
  const MAX_API_BYTES=131072;
  const ALLOWED_KINDS=new Set(['object','notebook','notebook-block','research-question','research-claim']);
  const text=v=>String(v??'');
  const clean=v=>text(v).replace(/[\u0000-\u001f\u007f]/g,' ').trim();
  const enc=v=>encodeURIComponent(clean(v));
  const dec=v=>{try{return decodeURIComponent(v);}catch(_){return v;}};
  const clone=v=>JSON.parse(JSON.stringify(v));
  function utf8Bytes(value){const s=text(value);if(typeof TextEncoder!=='undefined')return new TextEncoder().encode(s).length;let n=0;for(let i=0;i<s.length;i++){const c=s.charCodeAt(i);if(c<128)n++;else if(c<2048)n+=2;else if(c>=0xD800&&c<=0xDBFF&&i+1<s.length&&s.charCodeAt(i+1)>=0xDC00&&s.charCodeAt(i+1)<=0xDFFF){n+=4;i++;}else n+=3;}return n;}
  function durableReference(entry){
    if(!entry||!ALLOWED_KINDS.has(entry.kind)||!clean(entry.projectId)||!clean(entry.id))return null;
    const uri=`scw://project/${enc(entry.projectId)}/${enc(entry.kind)}/${enc(entry.id)}`;
    return {schema:DURABLE_REF_SCHEMA,uri,projectId:clean(entry.projectId).slice(0,160),kind:entry.kind,id:clean(entry.id).slice(0,160),notebookId:clean(entry.notebookId).slice(0,160),sectionId:clean(entry.sectionId).slice(0,160),authorization:false};
  }
  function parseDurableReference(value){const uri=typeof value==='string'?value:text(value&&value.uri);const m=/^scw:\/\/project\/([^/]+)\/([^/]+)\/([^/]+)$/.exec(uri);if(!m)return null;const kind=dec(m[2]);if(!ALLOWED_KINDS.has(kind))return null;return {schema:DURABLE_REF_SCHEMA,uri,projectId:dec(m[1]),kind,id:dec(m[3]),authorization:false};}
  function resolve(entries,reference){const r=parseDurableReference(reference);if(!r)return null;return (Array.isArray(entries)?entries:[]).find(e=>e&&e.projectId===r.projectId&&e.kind===r.kind&&e.id===r.id)||null;}
  function normalizeFields(input={}){return {summary:input.summary!==false,tags:Boolean(input.tags),provenance:Boolean(input.provenance),content:Boolean(input.content)};}
  function projectionData(entry,fields){const data={title:clean(entry.title).slice(0,500),projectTitle:clean(entry.projectTitle).slice(0,240),kind:entry.kind,subtype:clean(entry.subtype).slice(0,160),origin:clean(entry.origin).slice(0,240),updatedAt:clean(entry.updatedAt).slice(0,80)};if(fields.summary)data.summary=clean(entry.summary).slice(0,4000);if(fields.tags)data.tags=(Array.isArray(entry.tags)?entry.tags:[]).map(clean).filter(Boolean).slice(0,50);if(fields.provenance){const p=entry.provenance&&typeof entry.provenance==='object'?entry.provenance:{};data.provenance={sourceTitle:clean(p.sourceTitle).slice(0,500),sourceUrl:clean(p.sourceUrl).slice(0,2000),bibliography:p.bibliography&&typeof p.bibliography==='object'?clone(p.bibliography):{}};}if(fields.content)data.content=clean(entry.content).slice(0,50000);return data;}
  function createProjection(entry,options={},now=()=>new Date().toISOString(),id=()=>`projection-${Date.now()}`){const ref=durableReference(entry);if(!ref)return null;const fields=normalizeFields(options.fields||{});return {schema:PROJECTION_SCHEMA,id:clean(id()).slice(0,180),createdAt:now(),exposure:'public-readonly',durableRef:ref,fields,data:projectionData(entry,fields),sourceRevision:clean(entry.updatedAt).slice(0,80),fingerprint:'',governance:{explicitDisclosure:true,readOnly:true,canonicalSourceUnchanged:true,durableReferenceIsAuthorization:false,liveServerRead:false,automaticPublication:false,automaticRefresh:false}};}
  function stable(value){if(Array.isArray(value))return value.map(stable);if(value&&typeof value==='object'){const out={};Object.keys(value).sort().forEach(k=>out[k]=stable(value[k]));return out;}return value;}
  function fingerprintPayload(p){const x=clone(p);delete x.fingerprint;return JSON.stringify(stable(x));}
  function withFingerprint(p,fingerprint){const out=clone(p);out.fingerprint=clean(fingerprint).slice(0,160);return out;}
  function fingerprintAlgorithm(value){const f=clean(value);if(/^sha256:[0-9a-f]{64}$/i.test(f))return 'sha256';if(/^fnv1a32:[0-9a-f]{8}$/i.test(f))return 'fnv1a32';return '';}
  function fingerprintMatches(expected,actual){return Boolean(clean(expected))&&clean(expected)===clean(actual);}
  function validateProjection(p){const errors=[];if(!p||p.schema!==PROJECTION_SCHEMA)errors.push('schema');if(!parseDurableReference(p&&p.durableRef))errors.push('durable-ref');if(p&&p.exposure!=='public-readonly')errors.push('exposure');if(!p||!p.data||!clean(p.data.title))errors.push('title');if(!p||!p.governance||p.governance.readOnly!==true||p.governance.durableReferenceIsAuthorization!==false)errors.push('governance');return {ok:errors.length===0,errors};}
  function apiEnvelope(projection,workspaceVersion=''){if(!validateProjection(projection).ok)return null;const env={schema:API_SCHEMA,workspaceVersion:clean(workspaceVersion),readOnly:true,generatedAt:new Date().toISOString(),reference:clone(projection.durableRef),projection:clone(projection),links:{self:''},governance:{staticProjection:true,serverDataEndpoint:false,authenticationByReference:false,canonicalMutation:false,remoteWrite:false,credentialedFetch:false,corsCredentials:false}};return utf8Bytes(JSON.stringify(env))<=MAX_API_BYTES?env:null;}
  function normalizedOrigin(value){try{const u=new URL(clean(value));return u.origin;}catch(_){return '';}}
  function isLocalHostName(name){return name==='localhost'||name==='127.0.0.1'||name==='[::1]';}
  function rendererAssessment(rendererUrl,trustedOrigins=[]){const errors=[];let u=null;try{u=new URL(clean(rendererUrl));}catch(_){errors.push('renderer-url-invalid');return {ok:false,errors,origin:''};}if(u.username||u.password)errors.push('renderer-credentials-forbidden');if(u.protocol!=='https:'&&!(u.protocol==='http:'&&isLocalHostName(u.hostname)))errors.push('renderer-protocol');const trusted=(Array.isArray(trustedOrigins)?trustedOrigins:[trustedOrigins]).map(normalizedOrigin).filter(Boolean);if(trusted.length&&!trusted.includes(u.origin))errors.push('renderer-origin-untrusted');return {ok:errors.length===0,errors,origin:u.origin,protocol:u.protocol,trustedOrigins:trusted};}
  function embedDescriptor(projection,workspaceVersion='',rendererUrl='',options={}){const env=apiEnvelope(projection,workspaceVersion);if(!env)return null;const ra=rendererAssessment(rendererUrl,options.trustedOrigins||[]);const trustedOrigin=(ra.trustedOrigins&&ra.trustedOrigins[0])||ra.origin||'';return {schema:EMBED_SCHEMA,renderer:'sc-workspace-api-embed-v1',rendererUrl:clean(rendererUrl).slice(0,2000),payload:env,fingerprint:clean(projection.fingerprint),governance:{readOnly:true,staticPayload:true,noLiveDataFetch:true,identifierIsNotAuthorization:true,credentialedFetch:false,remoteMutation:false,postMessage:false,parentOriginImplicitTrust:false,trustedRendererOrigin:trustedOrigin,rendererOrigin:ra.origin,hardeningSchema:HARDENING_SCHEMA}};}
  function validateDescriptor(descriptor,options={}){const errors=[],warnings=[];if(!descriptor||descriptor.schema!==EMBED_SCHEMA)errors.push('descriptor-schema');if(descriptor&&descriptor.renderer!=='sc-workspace-api-embed-v1')errors.push('renderer-id');const p=descriptor&&descriptor.payload&&descriptor.payload.projection;if(!validateProjection(p).ok)errors.push('projection');if(!descriptor||!descriptor.governance||descriptor.governance.readOnly!==true||descriptor.governance.staticPayload!==true||descriptor.governance.noLiveDataFetch!==true)errors.push('embed-governance');if(descriptor&&descriptor.governance&&(descriptor.governance.credentialedFetch!==false||descriptor.governance.remoteMutation!==false||descriptor.governance.postMessage!==false))errors.push('integration-boundary');if(p&&clean(descriptor.fingerprint)!==clean(p.fingerprint))errors.push('fingerprint-binding');const trusted=options.trustedOrigins||descriptor?.governance?.trustedRendererOrigin||[];const ra=rendererAssessment(descriptor&&descriptor.rendererUrl,trusted);errors.push(...ra.errors);const bytes=utf8Bytes(JSON.stringify(descriptor||{}));if(bytes>MAX_EMBED_BYTES)errors.push('payload-too-large');if(!fingerprintAlgorithm(descriptor&&descriptor.fingerprint))warnings.push('fingerprint-format-unverifiable');return {ok:errors.length===0,errors:[...new Set(errors)],warnings:[...new Set(warnings)],bytes,rendererOrigin:ra.origin,maxBytes:MAX_EMBED_BYTES};}
  function apiAssessment(projection,computedFingerprint=''){const errors=[];const v=validateProjection(projection);if(!v.ok)errors.push(...v.errors.map(x=>'projection-'+x));if(!fingerprintAlgorithm(projection&&projection.fingerprint))errors.push('fingerprint-format');if(computedFingerprint&&!fingerprintMatches(projection&&projection.fingerprint,computedFingerprint))errors.push('fingerprint-mismatch');const env=apiEnvelope(projection,'');if(!env)errors.push('api-envelope-invalid-or-too-large');return {ok:errors.length===0,errors:[...new Set(errors)],fingerprintAlgorithm:fingerprintAlgorithm(projection&&projection.fingerprint),maxBytes:MAX_API_BYTES};}
  function integrationAssessment(projection,descriptor,computedFingerprint='',options={}){const api=apiAssessment(projection,computedFingerprint),embed=validateDescriptor(descriptor,options);return {schema:HARDENING_SCHEMA,ok:api.ok&&embed.ok,api,embed,boundaries:{readOnly:true,staticPayload:true,liveServerProjectApi:false,remoteWrite:false,credentialedFetch:false,postMessage:false,durableReferenceIsAuthorization:false,canonicalMutation:false}};}
  function safetyReport(assessment,projection,workspaceVersion='',now=()=>new Date().toISOString()){const a=assessment||{};return {schema:SAFETY_REPORT_SCHEMA,workspaceVersion:clean(workspaceVersion),generatedAt:now(),status:a.ok?'pass':'blocked',disclosureFields:normalizeFields(projection&&projection.fields||{}),fingerprintAlgorithm:fingerprintAlgorithm(projection&&projection.fingerprint)||'unverifiable',api:{ok:Boolean(a.api&&a.api.ok),errors:Array.isArray(a.api&&a.api.errors)?a.api.errors.slice(0,20):[]},embed:{ok:Boolean(a.embed&&a.embed.ok),errors:Array.isArray(a.embed&&a.embed.errors)?a.embed.errors.slice(0,20):[],warnings:Array.isArray(a.embed&&a.embed.warnings)?a.embed.warnings.slice(0,20):[],payloadBytes:Number(a.embed&&a.embed.bytes)||0,rendererOrigin:clean(a.embed&&a.embed.rendererOrigin).slice(0,300)},boundaries:{readOnly:true,staticPayload:true,liveServerProjectApi:false,remoteWrite:false,credentialedFetch:false,postMessage:false,durableReferenceIsAuthorization:false,canonicalMutation:false},privacy:{projectContentIncluded:false,projectTitleIncluded:false,durableReferenceIncluded:false,sourceUrlsIncluded:false,deviceIdentifierIncluded:false,userAgentIncluded:false,automaticUpload:false}};}
  function normalizeLedger(raw={}){const list=Array.isArray(raw.projections)?raw.projections:[];return {schema:LEDGER_SCHEMA,projections:list.filter(p=>validateProjection(p).ok).slice(0,200),updatedAt:clean(raw.updatedAt)};}
  function upsert(ledger,projection){const l=normalizeLedger(ledger),p=clone(projection);l.projections=[p,...l.projections.filter(x=>x.id!==p.id)].slice(0,200);l.updatedAt=new Date().toISOString();return l;}
  function remove(ledger,id){const l=normalizeLedger(ledger);l.projections=l.projections.filter(p=>p.id!==id);l.updatedAt=new Date().toISOString();return l;}
  function escJsonForHtml(v){return JSON.stringify(v).replace(/</g,'\\u003c').replace(/-->/g,'--\\u003e');}
  function embedHtml(descriptor,rendererUrl,options={}){if(!descriptor)return'';const d=clone(descriptor);if(rendererUrl)d.rendererUrl=clean(rendererUrl).slice(0,2000);const check=validateDescriptor(d,options);if(!check.ok)return'';const url=clean(d.rendererUrl);return `<div class="sc-workspace-embed" data-sc-workspace-embed><script type="application/json" data-sc-workspace-embed-data>${escJsonForHtml(d)}</script></div>${url?`<script src="${url.replace(/&/g,'&amp;').replace(/"/g,'&quot;')}"></script>`:''}`;}
  function renderFailure(host,code='invalid-payload'){if(!host)return false;host.textContent='';const box=document.createElement('div');box.setAttribute('role','status');box.setAttribute('data-sc-workspace-embed-error',clean(code).slice(0,80));box.style.cssText='box-sizing:border-box;border:1px solid #bbb;border-top:4px solid #000;background:#fff;color:#333;padding:16px;font-family:Arial,sans-serif;line-height:1.5;max-width:760px';const strong=document.createElement('strong');strong.textContent='Workspace embed unavailable.';const p=document.createElement('p');p.textContent='The static integration payload failed validation. No private Workspace data was fetched.';p.style.margin='6px 0 0';box.appendChild(strong);box.appendChild(p);host.appendChild(box);return false;}
  function renderElement(host,descriptor){if(!host)return false;const checked=validateDescriptor(descriptor);if(!checked.ok)return renderFailure(host,checked.errors[0]||'invalid-payload');const p=descriptor.payload.projection;host.textContent='';const card=document.createElement('article');card.setAttribute('aria-label',p.data.title);card.style.cssText='box-sizing:border-box;border:1px solid #d9d9d9;border-top:4px solid #000;background:#fff;color:#111;padding:20px;font-family:Montserrat,Arial,sans-serif;line-height:1.55;max-width:760px';const meta=document.createElement('div');meta.textContent=[p.data.kind,p.data.projectTitle].filter(Boolean).join(' · ');meta.style.cssText='font-size:11px;font-weight:800;letter-spacing:.08em;text-transform:uppercase;color:#666;margin-bottom:8px';card.appendChild(meta);const h=document.createElement('h3');h.textContent=p.data.title;h.style.cssText='font-size:24px;line-height:1.15;margin:0 0 10px;color:#000';card.appendChild(h);if(p.data.summary){const s=document.createElement('p');s.textContent=p.data.summary;s.style.cssText='margin:0 0 12px;color:#444';card.appendChild(s);}if(p.data.content){const c=document.createElement('div');c.textContent=p.data.content;c.style.cssText='white-space:pre-wrap;margin-top:14px;padding-top:14px;border-top:1px solid #e5e5e5;color:#222';card.appendChild(c);}if(Array.isArray(p.data.tags)&&p.data.tags.length){const t=document.createElement('div');t.textContent=p.data.tags.join(' · ');t.style.cssText='margin-top:12px;font-size:12px;color:#666';card.appendChild(t);}const f=document.createElement('div');f.textContent=p.durableRef.uri;f.style.cssText='margin-top:14px;padding-top:10px;border-top:1px solid #eee;font-size:10px;color:#777;overflow-wrap:anywhere';card.appendChild(f);host.appendChild(card);return true;}
  function autoRender(){if(typeof document==='undefined')return;const run=()=>document.querySelectorAll('[data-sc-workspace-embed]').forEach(host=>{if(host.dataset.scwRendered==='1')return;const node=host.querySelector('[data-sc-workspace-embed-data]');if(!node){renderFailure(host,'payload-missing');host.dataset.scwRendered='error';return;}try{const raw=node.textContent||'{}';if(utf8Bytes(raw)>MAX_EMBED_BYTES){renderFailure(host,'payload-too-large');host.dataset.scwRendered='error';return;}const d=JSON.parse(raw);if(renderElement(host,d))host.dataset.scwRendered='1';else host.dataset.scwRendered='error';}catch(_){renderFailure(host,'payload-json-invalid');host.dataset.scwRendered='error';}});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();}
  return {DURABLE_REF_SCHEMA,PROJECTION_SCHEMA,API_SCHEMA,EMBED_SCHEMA,LEDGER_SCHEMA,SAFETY_REPORT_SCHEMA,HARDENING_SCHEMA,STORAGE_KEY,MAX_EMBED_BYTES,MAX_API_BYTES,ALLOWED_KINDS,utf8Bytes,durableReference,parseDurableReference,resolve,normalizeFields,createProjection,fingerprintPayload,withFingerprint,fingerprintAlgorithm,fingerprintMatches,validateProjection,apiEnvelope,normalizedOrigin,rendererAssessment,embedDescriptor,validateDescriptor,apiAssessment,integrationAssessment,safetyReport,normalizeLedger,upsert,remove,embedHtml,renderFailure,renderElement,autoRender};
});
