(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.SCWorkspaceHome=api;})(typeof self!=='undefined'?self:this,function(){'use strict';const SCHEMA='sc-workspace-home-project-cockpit/1.0';function summarize(state){const projects=Array.isArray(state?.projects)?state.projects.filter(p=>!p.archivedAt):[];const active=projects.find(p=>p.id===state?.activeProjectId)||projects[0]||null;const objects=projects.reduce((n,p)=>n+(Array.isArray(p.objects)?p.objects.filter(o=>!o.archivedAt).length:0),0);const notebooks=projects.reduce((n,p)=>n+(Array.isArray(p?.notebooks?.notebooks)?p.notebooks.notebooks.length:0),0);const research=projects.reduce((n,p)=>n+(Array.isArray(p?.research?.questions)?p.research.questions.length:0)+(Array.isArray(p?.research?.claims)?p.research.claims.length:0),0);return {schema:SCHEMA,projectCount:projects.length,objectCount:objects,notebookCount:notebooks,researchCount:research,activeProject:active};}function nextAction(project){if(!project)return{key:'choose-project',title:'Choose or create a project',detail:'Home becomes project-specific once a local project is active.',view:'projects',mode:'overview'};const objects=(project.objects||[]).filter(o=>!o.archivedAt);const questions=project?.research?.questions||[];const notebooks=project?.notebooks?.notebooks||[];if(!questions.length&&!objects.length&&!notebooks.length)return{key:'frame-work',title:'Frame the first research question',detail:'The project is empty. Start with Research or capture the first source/evidence object.',view:'research',mode:'research'};if(!objects.length)return{key:'capture-evidence',title:'Capture supporting material',detail:'The project has context but no active Workspace Objects yet.',view:'projects',mode:'objects'};if(!project?.analysis?.questions?.length)return{key:'analyze',title:'Move evidence into analysis',detail:'Canonical objects exist; the Analysis workspace has not been started.',view:'projects',mode:'analysis'};if(!project?.briefing?.drafts?.length)return{key:'compose',title:'Turn the work into a briefing',detail:'The project has analytical structure but no briefing draft yet.',view:'projects',mode:'briefing'};return{key:'review',title:'Review the project state',detail:'Continue the active project or inspect Review for lifecycle and change context.',view:'projects',mode:'overview'};}return{SCHEMA,summarize,nextAction};});
