# Workspace v2.0.2 — Work Mode Cards, Cockpit Hierarchy & Navigation-State Repair

- Replaces the four Workspace Home Work Mode button/card hybrids with dedicated navigation-card styling.
- Adds equal-height 2×2 desktop layout and one-column mobile stacking.
- Adds visible `Open →` affordances, explicit active state, focus-visible state, and forced-colors support.
- Disables mode cards when no active project exists and changes the affordance to `Choose project`.
- Preserves the existing objects / analysis / decision / briefing routes and project-mode semantics.
- No storage/project/export schema changes and no canonical project mutation.
- Exact rollback baseline: v2.0.1.
