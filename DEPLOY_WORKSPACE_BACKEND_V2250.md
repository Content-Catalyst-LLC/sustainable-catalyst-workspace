# Deploy Workspace backend v2.25.0

Run `deploy_workspace_backend_v2_25_0_vps.sh` on the VPS with the backend ZIP. Migration 025 adds durable command receipts and the smoke test verifies command dispatch plus read-only server-generated query models.
