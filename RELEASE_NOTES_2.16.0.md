# Workspace v2.16.0

## Native Arrow / Parquet Cross-Language Exchange & Reproduction Verification

- Adds `sc-workspace-interchange-runtime` with PyArrow and 8 bounded operations.
- Promotes Arrow-compatible JSON descriptors to real Arrow IPC stream and Parquet artifacts.
- Adds migration 016 and durable interchange receipts.
- Adds artifact SHA-256 and Arrow schema fingerprints for reproduction verification.
- Adds API surfaces for interchange runtime status, operation registry, and receipts.
- Preserves Python, SQL, R, Julia, ML, and WASM runtime contracts and all v2.15 capabilities.
