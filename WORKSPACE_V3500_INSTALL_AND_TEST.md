# Workspace v3.5.0 Install and Test

1. Apply/push the repository package with `PUSH_WORKSPACE_V3500_FINAL.sh`.
2. Upload and execute the backend deploy package.
3. Install the Workspace v3.5.0 WordPress plugin.
4. Verify `/health`, the Catalyst Analytics R provider profile, the runtime package version, and `ReadonlyRootfs=true`.
