# Deploy Workspace Backend v2.23.0

Deploy the backend ZIP with `deploy_workspace_backend_v2_23_0_vps.sh`. It preserves the prior environment, applies migrations through 023, rebuilds the internal runtime fabric, validates inherited runtimes, executes a durable exponential failure-time analysis, verifies its reliability receipt, and checks the reliability runtime sandbox.
