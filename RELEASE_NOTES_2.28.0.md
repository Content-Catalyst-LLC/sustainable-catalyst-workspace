# Workspace v2.28.0 — Declarative Visualization Specification API

- Adds backend-authoritative renderer-neutral visualization specifications.
- Adds 13 bounded analytical view types and single/grid/dashboard analytical scenes.
- Adds explicit linked-view filter/highlight/select relationships.
- Resolves artifact, dataset, execution-run, receipt, and bounded inline-data sources into provenance-aware source pins.
- Adds deterministic SHA-256 specification fingerprints, revision history, and durable visualization-spec receipts.
- Includes visualization specifications in reproducible scientific study-package closure.
- Rejects renderer-specific executable fields and keeps arbitrary-code execution disabled.
- Preserves the restored full-size Workspace interface asset lineage.
