const assert=require('assert'),A=require('../wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-public-research-packages-v1.js');const p={id:'p1',title:'Project',objects:[{id:'e1',type:'evidence',title:'Evidence',summary:'Summary',content:'SECRET BODY',provenance:{source:'s1'}},{id:'d1',type:'dataset',title:'Data'}]};assert.equal(A.sha256Hex('abc'),'ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad');const d=A.buildPackage(p,['e1'],{title:'Package',license:'CC-BY-4.0'});assert.equal(d.visibility,'private-draft');assert.equal(d.objects.length,1);assert(!('content' in d.objects[0].fields));assert(d.objects[0].excludedFields.includes('content'));assert.throws(()=>A.releasePackage(d,{}));const r=A.releasePackage(d,{confirmPublish:true});assert.equal(r.visibility,'public-package');assert.equal(r.objects[0].visibility,'public-package');assert.equal(r.objects[0].provenance.sourceRecordId,'e1');const q=A.receipt(r);assert.equal(q.metadataOnly,true);assert.equal(q.projectContentIncluded,false);assert.equal(A.contract().governance.automaticPublication,false);console.log('PASS - v1.14.0 public research packages runtime');
