const assert=require('assert'),O=require('../wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-research-operations-v1.js');
const now='2026-08-15T17:00:00.000Z',old='2026-08-08T17:00:00.000Z';const op=O.createOperation({name:'Weekly source check',type:'source-update-check',cadence:'weekly',target:{key:'p1:source:s1',projectId:'p1',kind:'source',id:'s1',title:'Source 1'},createdAt:old},()=> 'op1',()=>old);const plan=O.planOperation(op,[{key:'p1:source:s1',projectId:'p1',kind:'source',id:'s1',title:'Source 1',origin:'library'}],now);assert.equal(plan.state,'due');const out=O.runOperation(op,[{key:'p1:source:s1',projectId:'p1',kind:'source',id:'s1',title:'Source 1',origin:'library'}],p=>p+'1',()=>now);assert(out.ok);assert.equal(out.run.governance.automaticNetworkRequest,false);assert(out.run.findings.some(x=>x.includes('not checked automatically')));const reviewed=O.transitionRun(out.run,'reviewed',()=>now);const receipt=O.buildReceipt(reviewed.run);assert.equal(receipt.schema,O.RECEIPT_SCHEMA);assert.equal(receipt.governance.metadataOnly,true);assert.equal(receipt.governance.containsProjectContent,false);assert.equal(receipt.governance.containsQueryText,false);assert.equal(receipt.governance.containsSourceUrls,false);assert(/^sha256:[0-9a-f]{64}$/.test(receipt.fingerprint));assert.equal(O.sha256Hex('abc'),'ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad');const blocked=O.planOperation({...op,target:{key:'missing'}},[],now);assert.equal(blocked.state,'blocked');const legacy=O.importLegacyRoutine({name:'Old verify',type:'verification-check',cadence:'monthly',target:{}},()=> 'legacy1',()=>now);assert.equal(legacy.type,'citation-verification');console.log('PASS - v1.10.0 Research Operations JS runtime');
