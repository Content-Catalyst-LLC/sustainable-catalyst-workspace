const assert=require('assert'),A=require('../wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-product-maturity-v1.js');const ev={};for(const d of A.DIMENSIONS)ev[d]={state:'ready',evidence:'validated'};const q=A.buildDossier(ev);assert.equal(q.overallState,'ready');assert.equal(q.numericScore,false);assert.equal(q.dimensions.length,12);assert.equal(A.candidateBoundary(q,{}).designation,'not-candidate');const c=A.candidateBoundary(q,{confirmCandidate:true});assert.equal(c.designation,'2.0-candidate');assert.equal(c.v2SchemaIntroduced,false);assert.equal(c.automaticPromotion,false);const blocked={...ev,'field-evidence':{state:'blocked',evidence:'open defect'}};assert.throws(()=>A.candidateBoundary(A.buildDossier(blocked),{confirmCandidate:true}));assert.equal(A.contract().automatic20Promotion,false);console.log('PASS - v1.15.0 product maturity runtime');
