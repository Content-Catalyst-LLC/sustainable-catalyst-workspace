const assert=require('assert');const A=require('../wordpress/sustainable-catalyst-workspace/assets/js/sc-workspace-workbench-decision-roundtrip-v1.js');
const project={id:'p1',title:'Infrastructure choice',description:'Project',objects:[{id:'d1',type:'dataset',title:'Demand',content:'1,2,3',provenance:{sourceType:'library'}},{id:'a1',type:'analysis',title:'Baseline',content:'analysis',provenance:{}},{id:'e1',type:'evidence',title:'Evidence',content:'evidence',provenance:{}},{id:'q1',type:'decision',title:'Decision frame',content:'choose',provenance:{}},{id:'s1',type:'source',title:'Source',content:'source',provenance:{}}]};
const wb={id:'h1',destination:'workbench'};const wc=A.buildContextPackage(project,wb,['d1','a1','q1'],{workspaceVersion:'1.6.0',destination:'workbench',workflow:'simulation',purpose:'Simulate demand',parameters:'n=100'});assert(wc);assert.equal(wc.schema,A.WB_CONTEXT);assert.deepEqual(wc.context.objectIds,['d1','a1']);assert.equal(wc.handoff.workflow,'simulation');assert.equal(wc.governance.automaticUpload,false);const wt=A.returnTemplate(wc);assert.equal(wt.destination,'workbench');assert(A.WB_FAMILIES.includes(wt.artifacts[0].family));const wr={schema:A.WB_RETURN,destination:'workbench',projectId:'p1',handoffId:'h1',sourceObjectIds:['d1'],artifacts:[{family:'simulation-result',title:'Simulation',content:'result',metadata:{parameters:'n=100',uncertainty:'p95'}}]};const wp=A.toWorkspaceReturnPacket(wr);assert(wp.ok);assert.equal(wp.packet.destination,'workbench');assert.equal(wp.packet.artifacts[0].type,'analysis');assert(wp.packet.artifacts[0].content.includes('Parameters: n=100'));
const ds={id:'h2',destination:'decision-studio'};const dc=A.buildContextPackage(project,ds,['q1','e1','s1','d1'],{workspaceVersion:'1.6.0',destination:'decision-studio',workflow:'scenario-comparison',purpose:'Compare options',scenarioNotes:'A vs B'});assert(dc);assert.equal(dc.schema,A.DS_CONTEXT);assert.deepEqual(dc.context.objectIds,['q1','e1','s1']);const dr={schema:A.DS_RETURN,destination:'decision-studio',projectId:'p1',handoffId:'h2',sourceObjectIds:['q1'],artifacts:[{family:'recommendation',title:'Recommendation',content:'Choose A',metadata:{scenario:'A vs B',risk:'cost'}}]};const dp=A.toWorkspaceReturnPacket(dr);assert(dp.ok);assert.equal(dp.packet.destination,'decision-studio');assert.equal(dp.packet.artifacts[0].type,'decision');assert(dp.packet.artifacts[0].content.includes('Scenario: A vs B'));assert(!A.normalizeReturn({schema:A.WB_RETURN,destination:'decision-studio',projectId:'p1',handoffId:'h2'}).ok);console.log('PASS - v1.6.0 Workbench/Decision Studio round-trip JS runtime');
