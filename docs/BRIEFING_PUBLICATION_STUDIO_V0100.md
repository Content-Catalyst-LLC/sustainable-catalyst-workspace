# v0.10.0 — Briefing & Publication Studio

Workspace can now convert connected research, evidence, analysis, decisions, and reusable objects into authored communication artifacts. The studio is deliberately a composition/export boundary rather than a CMS: it preserves source-object references, can materialize a canonical Document object, and can export Markdown, HTML, or a portable JSON publication package, but it does not perform automatic publication.
